import styled from "styled-components";
import {
  AuthBlock,
  AuthTop,
  Register,
} from "../../../LoginForm/components/LoginForm.styles";

export const Login = styled(Register)``;

export const RegisterBlock = styled(AuthBlock)``;

export const RegisterTop = styled(AuthTop)``;
