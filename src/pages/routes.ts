enum Paths {
  OTHER = "*",
  EMPTY = "/",
  HOME = "/im",
  VERIFY = "/registration/verify",
  REGISTER = "/registration",
  LOGIN = "/login",
  DIALOG = "/dialog/",
  DIALOGID = "/dialog/:id",
}

export default Paths;
